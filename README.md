# whispers

A static, local-only browser signal scan. Open `index.html` directly in a browser
or serve the folder with any static file server. No backend is required.

The page makes no external network calls, stores nothing persistently, and treats
unsupported or blocked browser capabilities as meaningful absences instead of
errors.
